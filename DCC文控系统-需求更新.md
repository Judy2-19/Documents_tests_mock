DCC 文控系统需求评审意见
（值得注意点）

评审对象	《DCC 文控系统需求说明书》V1.5.5（2026-07-22）；处置回写至 **V1.5.6～V1.5.8**
评审性质	需求 / 设计层面的问题清单，供产品与开发上线前对齐
优先级定义	P0 开发前必须解决（阻断性 / 影响数据模型）；P1 需求或设计需明确，否则易返工；P2 文档收口与一致性
结论概述	框架与合规内核扎实，可开工。**全部 T-01～T-15 文档侧已关闭**（含 T-01 会议定稿拆双字段，说明书 V1.5.8）。

一、总体评价（先说好的）
在提出问题前，先明确这份需求的底子是好的，评审意见不改变「可以开工」的整体判断：
1.合规内核抓得准：电子签名 + 服务器时间戳 + 岗位/权限快照 + 强制水印 + 全量审计日志 + 一键合规导出，直接对标实验室认可（CNAS/CMA）与 ISO 9001 文控要求。
2.版本状态机做对了：现行版本唯一、无确切生效日不得进入现行、新版生效旧版自动替代——这三条命门规则不仅写清楚，还落到了 §10.1 强制检查项 C1~C8 与验收要点。
3.对开发友好：方案 A 嵌入现网、技术栈对齐若依、dcc_ 表清单 + ER 图 + 外键/索引/建表顺序建议齐备。
以下问题按优先级排列，均给出「问题 / 影响 / 建议改法 / 涉及章节」，便于逐条处置。
二、P0 — 开发前必须解决
T-01　product_type_id 存在语义冲突（影响数据库设计）   [P0]　✅ 已关闭 · 会议定稿拆双字段（2026-07-23 / V1.5.8）
涉及章节：§8.2.3、§8.19、§19.1 → 已回写 §8.19.1、§19.1a
问题：同一字段被赋予两种互斥语义。§8.19 中 dcc_product_type 明确是业务领域（FOOD 食品检测 / ENV 环境 / PHARMA 医药），台账「产品类型」精准筛选一直用它；而 §19.1 又写「文件级别统一为三级，可沿用 product_type_id 映射 L1/L2/L3」。业务类型与文件级别是两个正交维度，塞进同一字段会直接打架。
影响：筛选、合规导出按级别打包（§8.18.3 scope=FILE_LEVEL）、培训矩阵匹配都会引用到这个字段，语义不统一将导致查询结果错乱、导出范围错误，且后期难以纠正。
建议改法：拆成两个独立字段——product_type_id（业务领域，保留 §8.19 语义）+ file_level（L1/L2/L3，§19.1 已提及的备选方案）。字典表相应拆分，dcc_product_type 不再兼作「文件级别」字典。此项须在建表前定稿。
定论：会议采纳拆双字段。`file_level`=L1/L2/L3；`product_type_id`=业务领域（预置示例：半导体检测、前沿检测、硅光芯片量产等，文控可扩展）。禁止字典兼代级别。
T-02　三级表单「免审批 + 每次保存主版本 +1」的版本策略需重新设计   [P0]　✅ 已关闭 · 方案 A（2026-07-23）
涉及章节：§1.5、§19.1、§19.4、§19.6 → 已回写 §4.2.2、§8.2.4a、§19.9
问题：三级表单可网页直接编辑、免审批，且每次保存主版本 +1。隐患一：版本号爆炸与语义混乱——常改表单很快到 20.0、50.0，而正式审批升版也是主版本 +1，两套动作共用同一版本体系。隐患二：合规口子——受控表单可不经审批随意改内容还自动升版，外审现场可能追问「表单变更由谁复核」。
影响：版本历史被高频保存淹没，审计可读性下降；受控/非受控边界模糊，存在被判「文件失控」的合规风险。
建议改法：为三级表单单列一套轻量版本标识（如 r1/r2 或时间戳版本），与审批升版的 1.0/2.0 体系区分；或采用「免审批快照 + 定期/触发式正式升版」的双层机制。至少明确：谁有权改、改动是否需要事后可见的复核痕迹。
定论：采纳方案 A——免审批保存递增轻量修订 `rN`，正式 `1.0/2.0` 仅审批升版可改；见说明书 V1.5.6。
T-03　合规导出的「任意时间点重建（as_of_time）」须先确认数据支撑能力   [P0]　✅ 已关闭 · 方案 B（2026-07-23）
涉及章节：§8.18.3、§8.18.4 → 已回写 §8.18.5、§19.9
问题：导出「截至某历史时刻」的现行版本清单、当时的分发对象、当时的审批状态，需要系统能重建历史快照。若业务表只存当前状态 + 审计日志，任意时点重建要么依赖完整事件流回放，要么只能近似。文档未说明历史数据是否足以支撑。
影响：这是系统的核心卖点之一。若数据模型不支持，上线后该功能只能给「当前状态」而非「历史时点状态」，与需求承诺不符，且事后补救成本极高（要回填历史）。
建议改法：在数据建模阶段明确实现路径二选一：(a) 关键状态变更点存快照表；(b) 审计日志保证足够完整以支持回放。据此评估各业务表是否需要增加时序/快照字段。此项决定核心功能可行性，须在设计阶段拍板。
定论：采纳方案 B——首期以完整审计/事件回放实现 as_of，不建全量快照表；日志缺口则导出失败，禁止静默近似；见说明书 V1.5.6。
三、P1 — 需求 / 设计需明确，否则易返工
T-04　「防泄密 / 防绕过打印」的定位与验收口径需修正   [P1]　✅ 已关闭（V1.5.7）
涉及章节：§1.2(7)、§5.4、§8.8.3、§8.17 → 已回写
问题：文档出现「禁止浏览器无水印静默打印」「拦截系统打印」等表述，纯 Web 环境无法硬拦截（截图、拍照、OS 打印均可绕过）。文档虽半承认「绕过仍以水印溯源」，但需求语言用了「禁止/拦截」，易被误解为能真防住。
影响：验收时若按「防住泄密」判定，将无法通过；也可能给业务方错误的安全预期。
建议改法：将定位统一为「震慑 + 溯源」而非防泄密。首期既已明确不做 DRM，就把 §1.2 目标(7) 与验收口径改为「强制水印可溯源 + 敏感操作留痕」，删除「防住/拦截」类不可兑现的承诺。
定论：统一为震慑 + 溯源；验收不以「防住绕过」为通过条件。
T-05　自研审批引擎的能力边界与「不接现网 BPM」的取舍需复评   [P1]　✅ 已关闭（V1.5.7）
涉及章节：§6、§7.1(need_qa_approve)、§1.5(审批终审) → 已回写 §6.5
问题：首期引擎仅 ANY/或签、ALL/会签，无条件路由、并行分支、加签/转办、超时升级。need_qa_approve 开关已预留但首期固定为否，说明质量批准节点迟早回归；届时「按分类做条件路由」简单引擎可能返工。且明确不接现网 BPM，意味着长期自维护。
影响：二期引入质量批准或复杂流程时可能重构；重复造轮子增加长期维护成本。
建议改法：明确记录该取舍的理由与二期演进路径；同时评估复用现网 BPM 换取条件路由/超时/加签能力的性价比，至少留出引擎可平滑接入外部流程的抽象层。
定论：写入不接 BPM 理由、首期边界、二期演进；业务经审批端口抽象，可替换引擎。
T-06　「到达生效日自动流转」的定时任务边界需补齐   [P1]　✅ 已关闭（V1.5.7）
涉及章节：§4.5、§10.1(C2) → 已回写 §4.5.1
问题：生效流转依赖每日定时扫描，因此「新版生效瞬间旧版失效」实为扫描粒度而非真瞬间。生效日 0 点的失效时机、时区、任务失败补偿、幂等（重复执行不产生重复分发/培训任务）等边界，§4.5 未展开。
影响：可能出现生效日当天旧版短时仍可打开、任务失败导致漏生效、重复执行造成脏数据。
建议改法：明确生效判定时点语义（建议以生效日 00:00 服务器时区为准）、扫描周期、失败重试与补偿机制，并要求生效/失效/培训任务生成全部幂等。
定论：服务器时区日历日判定；每小时扫描建议；失败重试+告警+补偿；生效/分发/培训幂等。
T-07　培训任务对「岗位数据」的外部依赖需显式化并校验   [P1]　✅ 已关闭（V1.5.7）
涉及章节：§8.16、§1(与 HR 无强耦合) → 已回写 §8.16.5
问题：培训任务按「岗位-文件矩阵」解析在岗人员，但文档强调与 HR 无强耦合、岗位来自组织快照。若现网组织中岗位数据不全或不准，培训任务目标人群就会错。
影响：合规必做的培训联动可能因基础数据质量问题而漏发/错发，影响完成率统计与培训证明可信度。
建议改法：把「依赖现网岗位数据的完整性与准确性」作为显式前置条件写入需求；解析结果为空/异常时告警文控；明确人员岗位变动后历史培训任务与证明的口径（已留 post_name 快照，建议补充「变动不影响历史证明」说明）。
定论：前置条件 + 解析预览/空结果告警 + 调岗不影响历史证明。
T-08　缺少存量文件「冷启动 / 迁移」方案   [P1]　✅ 已关闭（V1.5.7）
涉及章节：全文（首期范围 §13 未覆盖） → 已回写 §13.3
问题：首期上线时，历史纸质/电子受控文件如何初始导入、版本起点是否统一从 1.0 计、是否设双轨试运行期，需求中未提及。
影响：文控系统落地最痛点往往是存量迁移；缺方案会导致上线即面临「新系统空库、旧文件仍在线下流转」的双轨混乱。
建议改法：补充一节「上线数据初始化与迁移」：存量文件批量导入方式、历史版本起点规则、纸质受控份存量登记、试运行/双轨期与切换标准。
定论：§13.3 覆盖导入方式、版本起点、纸质存量、双轨试运行与切换标准。
四、P2 — 文档收口与一致性
T-09　密级字段注释与「仅两档」表述不一致   [P2]　✅ 已关闭（V1.5.7）
涉及章节：§1.5 vs §8.2.3 → 已对齐
问题：§1.5 定义密级仅「内部 / 机密」两档，但 §8.2.3 字段 security_level 注释仍写 PUBLIC / INTERNAL / SECRET（一般/内部/机密）。
影响：开发按字段注释实现会多出「一般/公开」档，与业务约定不符。
建议改法：统一为两档，字段注释同步收窄（枚举值可保留扩展位，但注释需说明首期仅启用 INTERNAL/SECRET）。
定论：首期仅 INTERNAL/SECRET；PUBLIC 仅预留扩展位。
T-10　数据域文字与字段取值不一致   [P2]　✅ 已关闭（V1.5.7）
涉及章节：§1.5、§5.2 vs §8.2.3(access_domain) → 已对齐
问题：文字表述「首期仅生产/研发」，字段 access_domain 取值为 ALL / PROD / RD（含 ALL）。
影响：「ALL」的适用场景未说明，开发对「全域可见文件」如何取值存在理解偏差。
建议改法：明确 ALL 的用途（文控/审计全域可见的文件是否用 ALL），或将文字改为「业务文件仅 PROD/RD，全域文件用 ALL」，消除歧义。
定论：业务文件 PROD/RD；跨域共用公司级文件用 ALL；文控/审计可见能力与角色有关，不等于 ALL。
T-11　同一规则多处重复，易改漏   [P2]　✅ 已关闭（V1.5.7）
涉及章节：强制水印、二次申请等散见 §5 / §7 / §10 / §19 → §10 定为权威
问题：核心规则在多章节反复出现，后续调整易遗漏其一，导致文档自相矛盾。
影响：需求维护成本升高，版本迭代中出现规则前后不一致的风险。
建议改法：设「全局强制规则」单一权威章节，其余章节引用而非复述；或在评审后做一次去重收口。
定论：§10 更名为「全局强制规则（权威章节）」；他处以引用为准。
T-12　审计日志保留年限需与质量体系对齐   [P2]　✅ 已关闭（V1.5.7）
涉及章节：§10(13)、§12 → 已改为默认 ≥6 年
问题：审计日志保留「≥3 年（配置）」。CNAS/CMA 对记录保存年限通常有明确要求，3 年可能偏短。
影响：若低于体系要求年限，外审可能判不符合项。
建议改法：与质量体系确认最低保存年限（可能为 6 年或与产品/项目生命周期挂钩），将默认值对齐后再配置化。
定论：默认 ≥6 年（可配，且不得低于质量体系确认值）；上线前书面确认。
T-13　签收前后的查看权限授予时点表述略绕   [P2]　✅ 已关闭（V1.5.7）
涉及章节：§4.5(5)、§8.7 → 已回写两段式
问题：员工「默认只能打开已分发给自己的文件」，签收后「获得该版本查看权限」；但签收前需预览满 N 秒才可签收，说明分发即已给预览权。授予时点（分发时 vs 签收后）表述不够清晰。
影响：开发可能实现成「签收前无法预览」，与「预览满 N 秒才可签收」的流程冲突。
建议改法：明确「分发即授予预览权（用于完成签收）、签收后授予持续查看权」的两段式表述，避免理解偏差。
定论：分发→预览权；签收→持续查看权。
T-14　外发外链 access_token 的安全细节需补充   [P2]　✅ 已关闭（V1.5.7）
涉及章节：§8.10 → 已回写 §8.10.1
问题：外发用 token 外链预览，若 token 置于 URL 可能经日志/Referer 泄露；一次性、绑定 IP、主动撤销等未细化。
影响：外链令牌泄露会导致受控文件在有效期内被非授权访问。
建议改法：补充 token 安全策略（避免明文置于 URL query、有效期 + 可撤销 + 访问审计），至少列为二期强化项。
定论：首期哈希存储、可撤销、访问审计、避免明文长期 query；绑 IP/一次性等列二期。
T-15　「10 秒内定位」是体验目标，非可客观判定的验收项   [P2]　✅ 已关闭（V1.5.7）
涉及章节：§1.2(8)、§8.2.2、§12、§14(8) → 已对齐
问题：10 秒含人工点选，取决于用户行为，与 P95<2s 的客观性能指标混在一起，验收时难以客观判定。
影响：验收争议：以「10 秒」作为硬性通过条件时，达标与否难以举证。
建议改法：把「检索 P95 < 2s」作为可判定验收指标；「10 秒定位」作为体验目标单列，不作为硬性通过/不通过条件。
定论：硬性=检索 P95 小于 2s；10 秒=体验目标。
五、处置建议一览
编号	标题	优先级	建议处置阶段	处置状态（2026-07-23）
T-01	product_type_id 语义冲突	P0	建表前定稿	**已关闭 · 拆双字段**（说明书 V1.5.8）
T-02	三级表单版本策略	P0	设计阶段定稿	**已关闭 · 方案 A**（说明书 V1.5.6）
T-03	合规导出时间点重建能力	P0	数据建模阶段拍板	**已关闭 · 方案 B**（说明书 V1.5.6）
T-04	防泄密/防绕过打印定位修正	P1	需求收口	**已关闭**（说明书 V1.5.7：震慑+溯源）
T-05	审批引擎能力边界与 BPM 取舍	P1	设计阶段复评	**已关闭**（说明书 V1.5.7：§6.5）
T-06	生效定时任务边界	P1	详设阶段	**已关闭**（说明书 V1.5.7：§4.5.1）
T-07	培训对岗位数据的依赖	P1	需求补充 + 详设	**已关闭**（说明书 V1.5.7：§8.16.5）
T-08	存量迁移/冷启动方案	P1	需求补充	**已关闭**（说明书 V1.5.7：§13.3）
T-09	密级字段注释一致性	P2	文档收口	**已关闭**（说明书 V1.5.7）
T-10	数据域取值一致性	P2	文档收口	**已关闭**（说明书 V1.5.7）
T-11	规则重复去重	P2	文档收口	**已关闭**（说明书 V1.5.7：§10 权威）
T-12	审计日志保留年限	P2	与质量体系确认	**已关闭**（默认 ≥6 年；上线前书面确认）
T-13	签收权限授予时点	P2	文档收口	**已关闭**（说明书 V1.5.7）
T-14	外发 token 安全	P2	二期强化	**已关闭**（首期基线 + 二期强化项，§8.10.1）
T-15	「10 秒定位」验收口径	P2	验收标准调整	**已关闭**（说明书 V1.5.7）

六、定论摘要
**P0**
1. T-01 → **会议定稿拆双字段**（V1.5.8）：`file_level` + `product_type_id`（业务领域：半导体检测 / 前沿检测 / 硅光芯片量产等）。
2. T-02 → 方案 A：三级表单轻量修订 rN，与正式 1.0/2.0 分离（V1.5.6）。
3. T-03 → 方案 B：完整审计/事件回放 as_of；缺口则导出失败（V1.5.6）。

**P1/P2（V1.5.7）**
4. T-04～T-15 均已按评审建议改写入说明书，详见 §19.9。

建议处置顺序：评审项文档侧 **全部关闭**；可按 V1.5.8 固化表结构并推进开发/原型对齐。

---

## 七、后续基线同步（非评审项 · V1.5.17 / 2026-07-24）

> 原型与说明书已同步至 **V1.5.17**（详见《DCC文控系统-需求说明书-V1.md》修订历史）。摘要如下，供对接与验收对照；**不改变** T-01～T-15 已关闭结论。

| 主题 | 约定 |
|---|---|
| 生效 / 分发 | 待生效仅文控台账可见；现行后文控→各部门负责人→本部门员工；须签收进「我的受控文件」 |
| 台账 vs 签收 | 台账全员可查；台账侧仅本部门非密可预览/下载/打印；已签收完整操作（含机密） |
| 可见性 | 变更单/变更通知/分发/借阅/外发/记录按角色与本人关联收口 |
| 纸质回收 | 仅曾签收过该文件的人员可点回收；丢失确认仅文控 |
| 合规 Excel | 导出含文件正文 + 审批记录 + 分发台账 + 分发签收台账；导入模板支持正文列/正文表 |
| 流程指南 | 工作台「使用者操作流程指南」对齐 V1.5.17 |
